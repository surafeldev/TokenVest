import '../styles/globals.css';
// import '@/styles/globals.css'; // Make sure the file path is correct and the file exists in the specified location.

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;